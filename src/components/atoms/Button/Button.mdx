import { Canvas, Meta, Story } from '@storybook/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import * as ButtonStories from './Button.stories';

<Meta title="Atoms/Button" of={ButtonStories} />

# Button

You know what a button is, I bet!

<Canvas>
  <Story of={ButtonStories.Default} />
</Canvas>

<Canvas>
  <div
    style={{
      display: 'flex',
      gap: 'var(--spacing-medium)',
      alignItems: 'center',
      flexWrap: 'wrap',
    }}
  >
    <Story of={ButtonStories.Primary} />
    <Story of={ButtonStories.Secondary} />
  </div>
</Canvas>

## Text

The easiest way to add text to your button is with the `text` attribute, but we also support adding text as a child element:

```jsx
<button text="Click me" />

<button>Also click me</button>
```

<ArgsTable story="Default" />
