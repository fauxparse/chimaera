import { Canvas, Meta, Story } from '@storybook/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import * as ProtonStories from './Proton.stories';

<Meta title="Atoms/Proton" of={ProtonStories} />

# Proton

A proton is the base component that everything else is built on.
It doesn’t do anything special by itself, just renders a `div`.

<Canvas>
  <Story of={ProtonStories.Default} />
</Canvas>

The main thing it lets you do is pass a custom component
using the `as` prop.

<Canvas>
  <Story of={ProtonStories.Button} />
</Canvas>

<ArgsTable />
