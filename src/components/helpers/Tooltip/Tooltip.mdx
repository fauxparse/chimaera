import { Canvas, Meta, Story } from '@storybook/blocks';
import { ArgsTable } from '@storybook/addon-docs';
import * as TooltipStories from './Tooltip.stories';

<Meta title="Helpers/Tooltip" of={TooltipStories} />

# Tooltip

<Canvas>
  <Story of={TooltipStories.Default} />
</Canvas>

## Delay

To control the delay of the tooltip, wrap it in a `FloatingDelayGroup`.

<Canvas>
  <Story of={TooltipStories.Group} />
</Canvas>
