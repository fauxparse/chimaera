import { render, screen } from '@testing-library/react';
import { describe, expect, it } from 'vitest';

import Tooltip from '.';

describe('Tooltip', () => {
  it('...', () => {
    render(<Tooltip />);
    // expect(...).toBe(...);
  });
});
